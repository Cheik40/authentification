const PrivatePage = () => {
    return (
        <div>
            <h1>Page privée (Admin)</h1>
            <p>Cette page est accessible uniquement aux administrateurs.</p>
        </div>
    );
};

export default PrivatePage;
