const ProtectedPage = () => {
    return (
        <div>
            <h1>Page protégée</h1>
            <p>
                Cette page est accessible uniquement aux utilisateurs connectés.
            </p>
        </div>
    );
};

export default ProtectedPage;
